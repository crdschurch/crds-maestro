defmodule CrossroadsInterface.GroupsToSigninTest do
  use CrossroadsInterface.ConnCase

  # test "given no referrer, conn should not change", %{conn: conn} do
  #   conn = CrossroadsInterface.Plug.GroupsToSignin.call(conn)
  #   assert conn == conn
  # end

end

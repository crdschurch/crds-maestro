import Countdown from '../../../web/static/js/home_page/countdown';

describe('Countdown', function () {
    var countdown;

    it("should load countdown if there is a countdown on the page", function() {
        countdown = new Countdown();

    });
})
